import React from "react"

import type {FC} from "react"

type Progress4Props = {
  progress: number
}

const Progress4: FC<Progress4Props> = ({progress}) => {
  return <div>hi</div>
}

export default Progress4
